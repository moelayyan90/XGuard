# Deployment refresh

This file exists to trigger the production deployment workflow after the Cloudflare compatibility-date fix. Production is considered live only after automated health, discovery, and MCP verification succeed.
