const escape = value => String(value ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
const shellQuote = value => "'" + String(value).replaceAll("'", "'\"'\"'") + "'";

export function onboardingPage(path, items, requested) {
  const paid = items.filter(item => item.pricing.amount_atomic !== "0");
  const selected = paid.find(item => item.id === requested) || paid.find(item => item.id === "feed-digest") || paid[0];
  const start = item => `/developers?capability=${encodeURIComponent(item.id)}#paid-execution`;
  const rows = items.map(item => `<tr><th scope="row"><a href="/capabilities/${escape(item.id)}">${escape(item.name)}</a></th><td>${item.pricing.amount_atomic === "0" ? "Free" : `${escape(item.pricing.amount)} USDC`}</td><td>${escape(item.pricing.delivered)}</td><td><a href="${item.pricing.amount_atomic === "0" ? "/try" : start(item)}">${item.pricing.amount_atomic === "0" ? "Try your HTML" : "Run this outcome"}</a></td></tr>`).join("");
  const paidSteps = selected ? `<section id="paid-execution"><div class="eyebrow">PAY FOR A LIVE RESULT</div><h2>Run ${escape(selected.name.toLowerCase())}.</h2><p>The current price is <strong>${escape(selected.pricing.amount)} USDC per execution</strong>. The command below caps authorization at that price and runs a real purchase. You need Node.js 20+ and your own wallet funded with USDC on Base.</p><ol><li>Download the source and install the official payment client:<pre>${escape("git clone https://github.com/moelayyan90/XGuard.git\ncd XGuard\nnpm install --no-save --package-lock=false @x402/core@2.24.0 @x402/evm@2.24.0 viem@2.37.6")}</pre></li><li>Make <code>XGUARD_PAYER_PRIVATE_KEY</code> available privately in your local process environment. Never enter a wallet key into this site or an AI conversation. Review the request below and replace the sample URLs with public sources you need.</li><li>Authorize one bounded purchase:<pre>${escape(`node sdk/examples/outcome-buy.mjs --pay --max-amount-atomic ${selected.pricing.amount_atomic} --request ${shellQuote(JSON.stringify(selected.example))}`)}</pre></li></ol><p>The client verifies the network, USDC asset and budget before signing. It returns structured output and saves a private recovery file when a payment identifier is available. If delivery is uncertain, recover using that file; do not start another purchase.</p><p><a href="https://github.com/moelayyan90/XGuard/blob/main/sdk/examples/outcome-buy.mjs">Review the purchase script</a> · <a href="https://github.com/moelayyan90/XGuard/blob/main/sdk/outcomes.js">Use the SDK in your own agent</a> · <a href="/pricing">Choose another outcome</a></p><p class="sub">There is no browser wallet checkout for these outcomes yet. Operator card credits purchase a separate Secretless Egress service.</p></section>` : `<section id="paid-execution"><h2>Paid execution is temporarily unavailable.</h2><p>Try the free parser while live payment configuration is unavailable. No paid request is offered here.</p><a class="button" href="/try">Try free</a></section>`;
  if (path === "/pricing") return {
    title: "XGuard pricing — Pay per live result", heading: "Choose your result.<br>See the exact price.",
    description: "Compare bounded page extraction, structured product offers and feed digests. Pay per execution in USDC with no XGuard subscription.",
    content: `<section id="prices"><h2>Per-execution pricing</h2><div class="table-scroll"><table><thead><tr><th>Outcome</th><th>Price</th><th>You receive</th><th>Start</th></tr></thead><tbody>${rows}</tbody></table></div><p>Prices include bounded source fallback. There is no XGuard subscription or minimum purchase for x402 outcomes. Wallet funding is separate.</p><h3>What happens if sources fail?</h3><p>If all sources fail after settlement, the response provides an execution credit for the same outcome. This is not an automatic cash refund. Keep the original recovery data.</p><h3>Need card payment for delegated API actions?</h3><p><a href="/pricing/operator">Operator Usage Credits</a> fund Secretless Egress only; they do not pay for the public-source outcomes above.</p></section>`,
  };
  if (path === "/developers" || path === "/connect") return {
    title: "XGuard developer quickstart — Free preview to paid execution", heading: "Get a result.<br>Then connect your agent.",
    description: "Try the parser free, choose a live outcome and run a budget-capped x402 purchase with the official payment client.",
    content: (selected ? `<section><h2>Already have a funded x402 agent?</h2><p>Connect your payment-capable MCP client to <code>https://api.xguardgate.com/mcp</code> and call <code>xguard_execute</code> with:</p><pre>${escape(JSON.stringify(selected.example, null, 2))}</pre><p>Your client receives the exact price and requests authorization through the standard x402 MCP payment flow. Configure its spending limit before enabling payment. A plain MCP installation does not include a wallet.</p><p><a href="https://docs.cdp.coinbase.com/x402/buyer/mcp-payments">Official x402 MCP client setup</a> · <a href="https://github.com/moelayyan90/XGuard#javascript-python-mcp-and-a2a">XGuard payment and recovery contract</a></p></section>` : "") + paidSteps,
  };
  if (path === "/try") return {
    title: "Try XGuard — Extract your HTML for free", heading: "Try your own HTML.<br>See the result free.",
    description: "Paste a small public HTML document to extract text, metadata and structured offers before choosing a paid live-source outcome.",
    content: `<section><h2>Ready to fetch live sources?</h2><p>The free preview processes supplied HTML without visiting a URL. Live outcomes fetch public sources and return coverage, source digests and a signed receipt.</p><a class="button" href="/pricing">Choose a live outcome</a> <a href="/developers#paid-execution">Run a paid request</a></section>`,
  };
  return { content: "" };
}

export const htmlPreviewInput = `<div class="preview-input"><label for="preview-html">Your public HTML (up to 12 KiB)</label><textarea id="preview-html" spellcheck="false" maxlength="12288" placeholder="&lt;main&gt;&lt;h1&gt;Your page&lt;/h1&gt;&lt;p&gt;Content to extract.&lt;/p&gt;&lt;/main&gt;"></textarea><p class="sub">HTML is sent to XGuard for parsing. Do not paste passwords, tokens or private documents.</p><button id="preview-own">Extract my HTML free</button></div>`;

export function previewClientScript(api) {
  return `const button=document.getElementById('demo'),own=document.getElementById('preview-own'),input=document.getElementById('preview-html'),status=document.getElementById('status'),result=document.getElementById('result');async function runPreview(body){if(button.disabled)return;button.disabled=true;if(own)own.disabled=true;status.textContent='Extracting HTML…';try{const response=await fetch('${api}/v1/execute',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.message||data.error||'Request failed');status.textContent='Extraction complete · free · no external source calls';result.hidden=false;result.textContent=JSON.stringify(data,null,2);document.getElementById('next-step').hidden=false;}catch(error){status.textContent=String(error.message);}finally{button.disabled=false;if(own)own.disabled=false;}}button.addEventListener('click',()=>runPreview({intent:'demo'}));if(own)own.addEventListener('click',()=>{if(!input.value.trim()){status.textContent='Paste some HTML first, or run the sample.';return;}if(new TextEncoder().encode(input.value).length>12288){status.textContent='Use at most 12 KiB of HTML.';return;}runPreview({intent:'demo',html:input.value});});`;
}
