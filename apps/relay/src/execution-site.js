import { NAME, VERSION, PROMISE, DESCRIPTION, API, MCP } from "./core/identity.js";
import { operationCatalog } from "./core/provider-operations.js";
const escape = value => String(value).replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
const code = value => `<pre><code>${escape(value)}</code></pre>`;
const instructions = { operation: "github.repository.read", capability: "<scoped capability>", input: { owner: "moelayyan90", repo: "XGuard" }, idempotency_key: "repository-read-001" };
const style = `:root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#17221f;background:#f5f4ed;--ink:#17221f;--accent:#d44b23;--line:#d2d7cb}*{box-sizing:border-box}body{margin:0}a{color:inherit;text-underline-offset:4px}nav,main,footer{max-width:1180px;margin:auto;padding:24px}nav{display:flex;gap:24px;align-items:center;border-bottom:1px solid var(--line)}nav strong{margin-right:auto;font-size:22px;letter-spacing:-1px}nav a{text-decoration:none;font-size:14px}main{padding-top:64px;padding-bottom:88px}.eyebrow{text-transform:uppercase;letter-spacing:.16em;font-size:12px;font-weight:700;color:#68746a}h1{font-size:clamp(40px,6.5vw,82px);line-height:1.03;letter-spacing:-.055em;max-width:850px;margin:22px 0 28px}h2{font-size:28px;letter-spacing:-.03em;margin-top:0}h3{font-size:18px}p,li{line-height:1.65}.lead{font-size:20px;max-width:730px;color:#516157}.actions{display:flex;flex-wrap:wrap;gap:12px;margin:30px 0}.button,button{display:inline-flex;align-items:center;justify-content:center;cursor:pointer;border:1px solid var(--ink);background:var(--ink);color:#fff;padding:14px 20px;border-radius:4px;text-decoration:none;font:inherit;font-size:14px;font-weight:650}.button.secondary,button.secondary{background:transparent;color:var(--ink)}button:disabled{opacity:.55;cursor:wait}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px;margin:34px 0}.card{border:1px solid var(--line);padding:25px;background:#fcfcf7;border-radius:6px;min-width:0}.dark{background:var(--ink);color:#f5f4ed;padding:30px;border-radius:6px}.dark p{color:#c4d2c3}pre{overflow:auto;white-space:pre-wrap;overflow-wrap:anywhere;background:#e9ede3;color:#17221f;padding:22px;border-radius:5px;line-height:1.6;font-size:13px}.dark pre{background:#23372f;color:#e6efdc}.steps{display:grid;grid-template-columns:repeat(5,1fr);padding:0;list-style:none;gap:12px;margin:48px 0}.steps li{border-top:3px solid var(--accent);padding-top:14px;font-size:14px}.steps b{display:block;font-size:12px;color:#68746a;margin-bottom:10px}section{margin-top:56px}label{display:block;margin:18px 0 8px;font-size:14px;font-weight:650}input,textarea,select{width:100%;font:inherit;padding:12px;border:1px solid #a6b1a3;border-radius:4px;background:#fff}textarea{font-family:ui-monospace,monospace;font-size:13px;line-height:1.5}input:focus,textarea:focus,select:focus,button:focus-visible,a:focus-visible{outline:3px solid #dd7954;outline-offset:3px}.two{display:grid;grid-template-columns:1fr 1fr;gap:28px}.muted{color:#657165;font-size:14px}.badge{font-size:12px;padding:5px 8px;border:1px solid #a0ad96;display:inline-block;border-radius:3px}.result{min-height:50px}table{border-collapse:collapse;width:100%}td,th{text-align:left;border-bottom:1px solid var(--line);padding:12px;font-size:14px}footer{border-top:1px solid var(--line);display:flex;flex-wrap:wrap;gap:18px;font-size:13px;padding-bottom:40px}@media(max-width:760px){nav{gap:14px;flex-wrap:wrap}nav strong{width:100%}.grid,.two{grid-template-columns:1fr}.steps{grid-template-columns:1fr 1fr}main{padding-top:36px}h1{font-size:46px}}`;
function page(path, title, body, script = "") {
  const nonce = crypto.randomUUID().replaceAll("-", "");
  const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${escape(DESCRIPTION)}"><link rel="canonical" href="https://xguardgate.com${path}"><title>${escape(title)} | XGuard</title><style>${style}</style></head><body><nav aria-label="Main navigation"><strong><a href="/">XGuard<span style="color:var(--accent)">↗</span></a></strong><a href="/developers">Developers</a><a href="/operators">Operators</a><a href="/pricing">Pricing</a><a href="/status">Status</a><a href="/connect">Connect MCP</a></nav><main>${body}</main><footer><span>${escape(NAME)} · ${VERSION}</span><a href="/security">Security</a><a href="/threat-model">Threat model</a><a href="/payment-lifecycle">Payment lifecycle</a><a href="/proofs">Proofs</a><a href="/incidents">Incidents</a><a href="/terms">Terms</a><a href="/privacy">Privacy</a><a href="/refund-policy">Refunds</a><a href="/.well-known/security.txt">Disclosure</a></footer>${script ? `<script nonce="${nonce}">${script}</script>` : ""}</body></html>`;
  return new Response(html, { headers: { "content-type": "text/html; charset=utf-8", "cache-control": path === "/operators" ? "no-store" : "public, max-age=120",
    "content-security-policy": `default-src 'none'; script-src 'nonce-${nonce}'; style-src 'unsafe-inline'; connect-src ${API}; base-uri 'none'; frame-ancestors 'none'; form-action 'none'`, "referrer-policy": "no-referrer" } });
}
const apiScript = `const API='${API}';const $=id=>document.getElementById(id);async function call(path,body,key,method='POST'){const response=await fetch(API+path,{method,headers:{'content-type':'application/json',...(key?{'x-xguard-key':key}:{})},...(body===undefined?{}:{body:JSON.stringify(body)}),cache:'no-store'});const value=await response.json();if(!response.ok)throw new Error(value.error?.message||value.error?.code||value.error_code||value.error||('HTTP '+response.status));return value;}function show(id,value){$(id).textContent=typeof value==='string'?value:JSON.stringify(value,null,2);}`;
const policies = {
  "/threat-model": ["An explicit trust boundary", `<p>The operator, XGuard runtime and upstream provider remain trusted. The agent and provider response are untrusted. An agent receives a bearer capability scoped to an origin, path, method, expiration, call budget and optional operation/resource allowlist. Possession authorizes only those operations.</p><p>The gateway checks scope and reserves idempotency before billing; a committed debit precedes upstream credential decryption. Private destinations, redirects, method changes, reflected credentials and oversized results are rejected. Ambiguous writes are not retried.</p><p>XGuard cannot prevent an operator from sharing a provider key elsewhere, remove secrets from a malicious provider's own logs, or promise correctness of provider content. Gateway credit budgets do not cap a provider's invoice; bounded model requests also require provider-side project budgets.</p>`],
  "/payment-lifecycle": ["Payment before execution", `<p>A signed quote binds the input, network, asset, recipient, amount, expiry and limits. A payment authorization is reserved durably, verified, and reserved for settlement before transmission. Execution starts only after settlement evidence is committed.</p><p>If settlement is uncertain, the operation requires reconciliation. Recovery reads chain receipts and checks the authorization nonce and exact token transfer. It never switches facilitators or resubmits an uncertain settlement. Missing evidence remains unresolved. Successful settlement followed by failed public-source execution produces a bound recovery credit.</p><p>Discovery, demos, quotes, 402 responses, testnet payments and self-payments are not recognized revenue. Production external settlement is cash received; revenue is recognized once the result is delivered.</p>`],
  "/proofs": ["Verify what was executed", `<p>The result includes an execution identifier, request digest, result digest and signed ProofRail evidence. Use <code>xguard_verify_receipt</code> or <code>POST /v1/receipts/verify</code> with the proof and result digest. Public paid outcomes can also include the bound x402 receipt.</p><p>A valid signature proves that XGuard signed the evidence and that matching bytes have not changed. It does not establish the factual truth of a web page or model response. Save the response and proof in your own records; capability result recovery currently lasts only while the capability remains valid.</p>`],
  "/incidents": ["Observed service state", `<p><a href="/status">Current status</a> shows real measurements from the active release. Empty or unavailable data is labeled explicitly. This page makes no historical uptime claim.</p><p>For an uncertain paid request, preserve its identifier and quote; retrieve the existing result instead of creating a new payment. For a capability-backed write, preserve the exact input and idempotency key. Do not retry with a new key.</p><p>Public incident reports are tracked in the <a href="https://github.com/moelayyan90/XGuard/issues?q=is%3Aissue+label%3Aproduction-incident">production incident tracker</a>. Never include credentials, capabilities, payment signatures or private provider responses in an issue.</p>`],
};
export function executionPage(path) {
  if (path === "/") return page(path, "Agent Execution Gateway", `<p class="eyebrow">Agent execution / credentials stay server-side</p><h1>Give agents capabilities,<br>not reusable credentials.</h1><p class="lead">Let an agent do one authorized job. XGuard checks the policy and budget, authenticates the provider request, and returns the result with verifiable evidence.</p><div class="actions"><a class="button" href="/demo/secretless">Run a real capability</a><a class="button secondary" href="/connect">Connect MCP</a><a class="button secondary" href="/try">Try a free public outcome</a></div><p class="muted">The controlled demo is free. Your provider credentials stay out of prompts and tool arguments.</p><ol class="steps"><li><b>01 / OPERATOR</b>Store a credential encrypted</li><li><b>02 / AGENT</b>Receive a scoped capability</li><li><b>03 / POLICY</b>Check limits and commit billing</li><li><b>04 / EXECUTION</b>Inject the credential server-side</li><li><b>05 / EVIDENCE</b>Return a durable result and proof</li></ol><div class="two"><section class="dark"><span class="badge">The agent receives permission</span><h2 style="margin-top:22px">One call.<br>A bounded operation.</h2>${code(JSON.stringify(instructions, null, 2))}</section><section><p class="eyebrow">Explicit by design</p><h2>Know what an agent can do.</h2><p>Choose the provider, operation, resource, lifetime and call budget. Revoke a capability when the job is over. Exact retries return the stored result.</p><p>Read a repository. Create a draft pull request. Post to an allowed channel. Run a bounded model request. Each adapter has a published schema and required scope.</p><a href="/developers">Explore supported operations ↗</a></section></div><section><h2>Useful public examples</h2><div class="grid"><article class="card"><h3>Extract page evidence</h3><p>A free local preview, then bounded paid extraction when you need live sources.</p><a href="/try">Try extraction</a></article><article class="card"><h3>Compare product offers</h3><p>Structured offers from up to three public sources. Inspect the exact price before paying.</p><a href="/capabilities/product-offers">View example</a></article><article class="card"><h3>Merge public feeds</h3><p>A deduplicated feed digest with source evidence, a receipt and proof.</p><a href="/capabilities/feed-digest">View example</a></article></div></section>`);
  if (path === "/connect") return page(path, "Connect your agent", `<p class="eyebrow">One canonical MCP endpoint</p><h1>Connect. Delegate.<br>Execute.</h1><p class="lead">Discovery is public and stateless. Credential-backed execution requires a scoped capability from your operator.</p>${code(MCP)}<div class="grid"><section class="card"><h3>Claude Code</h3>${code(`claude mcp add xguard --transport http ${MCP}`)}</section><section class="card"><h3>Codex</h3>${code(`[mcp_servers.xguard]\nurl = "${MCP}"`)}</section><section class="card"><h3>Cursor</h3>${code(JSON.stringify({ mcpServers: { xguard: { url: MCP } } }, null, 2))}</section></div><h2>VS Code</h2>${code(JSON.stringify({ servers: { xguard: { type: "http", url: MCP } } }, null, 2))}<h2>Start free</h2>${code('xguard_execute {"intent":"demo"}')}<p>For a provider call, <a href="/operators">create a scoped capability</a>, then call <code>xguard_secretless_call</code>. Keep operator and provider keys outside the conversation. The legacy <code>xguard_egress_fetch</code> and <code>xguard.web.fetch</code> routes remain available with signed prices.</p>`);
  if (path === "/developers") return page(path, "Developer contract", `<p class="eyebrow">Exact operations / explicit scopes</p><h1>A small contract<br>for real work.</h1><p class="lead">Preflight the policy, execute with a capability, then verify the proof. Mutations require a stable idempotency key.</p><div class="actions"><a class="button" href="${API}/openapi.json">OpenAPI</a><a class="button secondary" href="${API}/v1/providers/operations">Operation schemas</a><a class="button secondary" href="https://github.com/moelayyan90/XGuard/tree/main/sdk/examples">SDK examples</a></div>${code(`curl ${API}/v1/secretless/call -H 'content-type: application/json' -d '${JSON.stringify(instructions)}'`)}<table><thead><tr><th>Operation</th><th>Effect</th><th>Provider permission</th></tr></thead><tbody>${operationCatalog().map(op => `<tr><td><code>${escape(op.id)}</code></td><td>${op.classification}</td><td>${escape(op.scope)}</td></tr>`).join("")}</tbody></table><p>Adapters require your own permitted provider credential. Provider invoices are separate from XGuard Usage Credits. Cloudflare deployments and Stripe money movement are not supported. Responses are bounded to 48 KiB; oversized or ambiguous results are not repeated.</p><section><h2>Client configuration downloads</h2><p><a href="/install/cursor.json">Cursor JSON</a> · <a href="/install/vscode.json">VS Code JSON</a> · <a href="/install/claude-code.json">Claude Code JSON</a> · <a href="/install/codex.toml">Codex TOML</a></p><p>MCP discovery does not supply a funded wallet or authorize provider access. Obtain a scoped capability from your operator for credential-backed calls.</p></section><section id="paid-execution"><h2>Public outcome examples</h2><p><a href="/try">Free public preview</a> accepts your own HTML without a wallet. Live extraction, product offers and feed digests use the separate signed x402 payment flow.</p><p>The existing purchase example reads <code>XGUARD_PAYER_PRIVATE_KEY</code> only from your local environment. Never paste the private key into an agent conversation. Inspect the signed price and exact recipient before authorizing payment.</p>${code(`node sdk/examples/outcome-buy.mjs --pay --max-amount-atomic 6000 --request '{"capability":"product-offers","input":{"urls":["https://your-public-store.example/product"]}}'`)}<p><a href="/pricing">Published outcome prices</a> · <a href="https://github.com/moelayyan90/XGuard/blob/main/sdk/examples/outcome-buy.mjs">Purchase example and required input</a></p></section>`);
  if (path === "/demo/secretless") return page(path, "Run a real capability", `<p class="eyebrow">Controlled authenticated service / free</p><h1>See the boundary<br>in action.</h1><p class="lead">A fresh server-side key protects a read-only fixture. Your browser receives a two-minute, one-call capability. The demo executes it, tests a forbidden path, retrieves the stored result and verifies its proof.</p><p>No external provider is contacted, no payment is made, and no reusable credential is returned.</p><button id="run">Run the capability demo</button><pre id="result" class="result" aria-live="polite">Ready for a real scoped request.</pre>`, `${apiScript}$('run').onclick=async()=>{$('run').disabled=true;show('result','Creating a scoped capability…');try{const grant=await call('/v1/demo/secretless',{});const input={capability:grant.capability,target:grant.target,method:'GET',idempotency_key:grant.idempotency_key};const first=await call('/v1/secretless/call',input);let scopeBlocked=false;try{await call('/v1/secretless/call',{...input,target:'https://demo.xguardgate.com/v1/forbidden'});}catch{scopeBlocked=true;}const replay=await call('/v1/secretless/call',input);const verified=await call('/v1/receipts/verify',{proof:first.proof,result_sha256:first.receipt.result_sha256});if(!scopeBlocked||!verified.valid||replay.request_id!==first.request_id||!replay.receipt.replay)throw new Error('The demo did not satisfy every verification check.');show('result',{authenticated:first.result.authenticated,secret_returned:first.result.secret_returned,out_of_scope_blocked:scopeBlocked,replay_same_execution:true,proof_valid:verified.valid,result:first.result,receipt:first.receipt,proof:first.proof});}catch(e){show('result',e.message);}finally{$('run').disabled=false;}};`);
  if (path === "/operators") return page(path, "Operator onboarding", `<p class="eyebrow">Operator only / keep keys outside agent context</p><h1>Delegate the job.<br>Keep the credential.</h1><p class="lead">Use a provisioned XGuard operator key and a provider key limited to the intended resource. Need credits? <a href="/pricing/operator">Open operator checkout</a>.</p><div class="two"><div class="card"><label for="key">XGuard operator key</label><input id="key" type="password" autocomplete="off"><label for="secret">Provider credential</label><input id="secret" type="password" autocomplete="off"><label for="operation">Allowed operation</label><select id="operation">${operationCatalog().map(op => `<option>${op.id}</option>`).join("")}</select><label for="input">Operation input (JSON)</label><textarea id="input" rows="7">{"owner":"moelayyan90","repo":"XGuard"}</textarea><div class="two"><div><label for="ttl">Lifetime in seconds</label><input id="ttl" type="number" min="30" max="3600" value="300"></div><div><label for="calls">Maximum calls</label><input id="calls" type="number" min="1" max="1000" value="1"></div></div><label for="budget">Maximum total XGuard credits</label><input id="budget" type="number" min="1" value="1"><div class="actions"><button id="create">Store credential and issue capability</button></div><p class="muted">Keys are held only in this page's memory and sent to the canonical API. They are never saved in browser storage or included in the agent request.</p></div><div><h2>Your agent request</h2><pre id="request">Create a capability to see the exact request.</pre><div class="actions"><button id="copy" disabled>Copy request</button><button id="execute" disabled>Execute this operation</button><button id="revoke" class="secondary" disabled>Revoke capability</button></div><pre id="result" aria-live="polite">The target, method and resource are derived from the explicit operation. Inspect the request before executing a write.</pre></div></div>`, `${apiScript}let agentRequest,capId,operatorKey;$('create').onclick=async()=>{$('create').disabled=true;try{operatorKey=$('key').value;const operation=$('operation').value,input=JSON.parse($('input').value);const plan=await call('/v1/providers/plan',{operation,input});const price=await call('/v1/egress/pricing',undefined,null,'GET');const credential=await call('/v1/egress/credentials',{provider:plan.provider,value:$('secret').value,allowed_paths:[plan.path_prefix],allowed_methods:[plan.method]},operatorKey);$('secret').value='';const cap=await call('/v1/egress/capabilities',{credential_id:credential.credential.id,target_origin:plan.target_origin,path_prefix:plan.path_prefix,allowed_methods:[plan.method],allowed_operations:[operation],operation_limits:{resources:[plan.context.resource],...(plan.context.max_output_tokens?{max_output_tokens:plan.context.max_output_tokens}:{})},ttl_seconds:Number($('ttl').value),max_calls:Number($('calls').value),max_credits_per_call:price.credits_per_authorized_egress_attempt,max_total_credits:Number($('budget').value)},operatorKey);capId=cap.capability_id;agentRequest={operation,input,capability:cap.capability,idempotency_key:crypto.randomUUID()};show('request',agentRequest);show('result','Capability expires '+cap.expires_at+'. Each authorized attempt costs '+price.credits_per_authorized_egress_attempt+' XGuard credit(s), plus separate provider charges.');for(const id of ['copy','execute','revoke'])$(id).disabled=false;}catch(e){show('result',e.message);}finally{$('create').disabled=false;}};$('copy').onclick=()=>navigator.clipboard.writeText(JSON.stringify(agentRequest,null,2)).catch(()=>show('result','Select and copy the request above.'));$('execute').onclick=async()=>{$('execute').disabled=true;try{show('result',await call('/v1/secretless/call',agentRequest));}catch(e){show('result',e.message);}finally{$('execute').disabled=false;}};$('revoke').onclick=async()=>{try{show('result',await call('/v1/egress/capabilities/'+capId,undefined,operatorKey,'DELETE'));$('execute').disabled=true;$('revoke').disabled=true;}catch(e){show('result',e.message);}};`);
  if (path === "/status") return page(path, "Observed service status", `<p class="eyebrow">Measured by the active release</p><h1>Observed,<br>not assumed.</h1><p class="lead">Request success and latency are separated by traffic class. Empty measurements mean no observations. No uptime percentage is inferred.</p><button id="refresh">Refresh measurements</button><pre id="result" aria-live="polite">Loading real observations…</pre>`, `${apiScript}async function refresh(){try{show('result',await call('/v1/status',undefined,null,'GET'));}catch(e){show('result',e.message);}}$('refresh').onclick=refresh;refresh();`);
  if (policies[path]) return page(path, policies[path][0], `<p class="eyebrow">XGuard / operating contract</p><h1>${policies[path][0]}</h1>${policies[path][1]}`);
  return null;
}
