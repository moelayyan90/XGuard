{
  "name": "@xguard/worker",
  "version": "0.1.0-alpha.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@x402/core": "2.22.0",
    "@xguard/core": "0.1.0-alpha.0",
    "ajv": "8.20.0",
    "hono": "4.13.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.21.3",
    "typescript": "5.9.3",
    "wrangler": "4.123.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:mainnet": "wrangler deploy --config wrangler.mainnet.jsonc",
    "build": "wrangler deploy --dry-run --outdir dist",
    "build:mainnet": "wrangler deploy --dry-run --config wrangler.mainnet.jsonc --outdir dist-mainnet",
    "types": "wrangler types",
    "test": "vitest run --config ../../vitest.worker.config.ts",
    "build:economic-preview": "wrangler deploy --dry-run --config wrangler.economic-preview.jsonc --outdir dist-economic-preview"
  }
}
