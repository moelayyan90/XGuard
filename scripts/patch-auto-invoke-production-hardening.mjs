import { readFileSync, writeFileSync } from "node:fs";

const sourcePath = "apps/worker/src/auto-invoke.ts";
let source = readFileSync(sourcePath, "utf8");

// Do not inspect a request body for provider inference until the standard
// client supplied a syntactically valid XGuard merchant credential.
const modelOld = `  if (!isOpenAiCompatiblePath(path)) return null;\n  const model = await readModel(request);`;
const modelNew = `  if (!isOpenAiCompatiblePath(path)) return null;\n  const model =\n    merchantTokenFromStandardClient(request) === null\n      ? null\n      : await readModel(request);`;
if (source.includes(modelOld)) source = source.replace(modelOld, modelNew);
else if (!source.includes(modelNew)) throw new Error("model inference block not found");

// Preserve provider execution success even if fee accounting becomes
// temporarily unavailable. A client retry after a successful provider call
// can duplicate external work, so accounting falls back to release/pending.
const networkOld = `  } catch {\n    await releaseGatewayFee(\n      env.DB,\n      access.merchant.merchantId,\n      reservation.eventKey,\n    ).catch(() => undefined);\n    return autoResponse(\n      { error: "upstream_unavailable", provider: route.provider },\n      502,\n      requestId,\n      0,\n      route.provider,\n      0,\n    );\n  }`;
const networkNew = `  } catch {\n    const accounting = await releaseAutoInvokeReservation(\n      env.DB,\n      access.merchant.merchantId,\n      reservation.eventKey,\n    );\n    return autoResponse(\n      { error: "upstream_unavailable", provider: route.provider },\n      502,\n      requestId,\n      0,\n      route.provider,\n      0,\n      accounting,\n    );\n  }`;
if (source.includes(networkOld)) source = source.replace(networkOld, networkNew);
else if (!source.includes(networkNew)) throw new Error("network failure block not found");

const nonBillableOld = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (!isAutoInvokeBillableStatus(upstream.status)) {\n    await releaseGatewayFee(\n      env.DB,\n      access.merchant.merchantId,\n      reservation.eventKey,\n    ).catch(() => undefined);\n    return proxiedResponse(upstream, requestId, 0, route.provider, latencyMs);\n  }\n\n  await earnGatewayFee(env.DB, {\n    merchantId: access.merchant.merchantId,\n    eventKey: reservation.eventKey,\n    upstreamStatus: upstream.status,\n    latencyMs,\n    requestBytes: contentLength(request.headers),\n    responseBytes: contentLength(upstream.headers),\n  });\n  return proxiedResponse(\n    upstream,\n    requestId,\n    feeMicroUsd,\n    route.provider,\n    latencyMs,\n  );`;
const nonBillableNew = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (isAutoInvokeRedirectStatus(upstream.status)) {\n    const accounting = await releaseAutoInvokeReservation(\n      env.DB,\n      access.merchant.merchantId,\n      reservation.eventKey,\n    );\n    return autoResponse(\n      {\n        error: "upstream_redirect_rejected",\n        provider: route.provider,\n        upstreamStatus: upstream.status,\n      },\n      502,\n      requestId,\n      0,\n      route.provider,\n      latencyMs,\n      accounting,\n    );\n  }\n\n  if (!isAutoInvokeBillableStatus(upstream.status)) {\n    const accounting = await releaseAutoInvokeReservation(\n      env.DB,\n      access.merchant.merchantId,\n      reservation.eventKey,\n    );\n    return proxiedResponse(\n      upstream,\n      requestId,\n      0,\n      route.provider,\n      latencyMs,\n      accounting,\n    );\n  }\n\n  const accounting = await finalizeAutoInvokeSuccess(env.DB, {\n    merchantId: access.merchant.merchantId,\n    eventKey: reservation.eventKey,\n    upstreamStatus: upstream.status,\n    latencyMs,\n    requestBytes: contentLength(request.headers),\n    responseBytes: contentLength(upstream.headers),\n  });\n  return proxiedResponse(\n    upstream,\n    requestId,\n    accounting === "earned" ? feeMicroUsd : 0,\n    route.provider,\n    latencyMs,\n    accounting,\n  );`;
if (source.includes(nonBillableOld)) source = source.replace(nonBillableOld, nonBillableNew);
else if (!source.includes("upstream_redirect_rejected") || !source.includes("finalizeAutoInvokeSuccess"))
  throw new Error("provider accounting block not found");

const statusOld = `export function isAutoInvokeBillableStatus(status: number): boolean {\n  return Number.isInteger(status) && status >= 200 && status < 300;\n}\n`;
const statusNew = `${statusOld}\nexport function isAutoInvokeRedirectStatus(status: number): boolean {\n  return Number.isInteger(status) && status >= 300 && status < 400;\n}\n\nexport type AutoInvokeAccountingState =\n  | "earned"\n  | "released"\n  | "pending-release";\n\nexport async function finalizeAutoInvokeSuccess(\n  db: D1Database,\n  input: {\n    merchantId: string;\n    eventKey: string;\n    upstreamStatus: number;\n    latencyMs: number;\n    requestBytes?: number;\n    responseBytes?: number;\n  },\n): Promise<AutoInvokeAccountingState> {\n  try {\n    await earnGatewayFee(db, input);\n    return "earned";\n  } catch {\n    return releaseAutoInvokeReservation(db, input.merchantId, input.eventKey);\n  }\n}\n\nexport async function releaseAutoInvokeReservation(\n  db: D1Database,\n  merchantId: string,\n  eventKey: string,\n): Promise<Exclude<AutoInvokeAccountingState, "earned">> {\n  try {\n    await releaseGatewayFee(db, merchantId, eventKey);\n    return "released";\n  } catch {\n    return "pending-release";\n  }\n}\n`;
if (!source.includes("export async function finalizeAutoInvokeSuccess")) {
  if (!source.includes(statusOld)) throw new Error("billable status helper not found");
  source = source.replace(statusOld, statusNew);
}

const proxySigOld = `function proxiedResponse(\n  upstream: Response,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: ProviderId,\n  latencyMs: number,\n): Response {`;
const proxySigNew = `function proxiedResponse(\n  upstream: Response,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: ProviderId,\n  latencyMs: number,\n  accounting: AutoInvokeAccountingState,\n): Response {`;
if (source.includes(proxySigOld)) source = source.replace(proxySigOld, proxySigNew);
else if (!source.includes(proxySigNew)) throw new Error("proxiedResponse signature not found");

const proxyHeaderOld = `  headers.set("X-XGuard-Upstream-Latency-Ms", String(latencyMs));\n  headers.set("X-Content-Type-Options", "nosniff");`;
const proxyHeaderNew = `  headers.set("X-XGuard-Upstream-Latency-Ms", String(latencyMs));\n  headers.set("X-XGuard-Accounting", accounting);\n  headers.set("X-Content-Type-Options", "nosniff");`;
if (source.includes(proxyHeaderOld)) source = source.replace(proxyHeaderOld, proxyHeaderNew);
else if (!source.includes('headers.set("X-XGuard-Accounting", accounting)'))
  throw new Error("proxiedResponse headers not found");

const autoOld = `function autoResponse(\n  value: Record<string, unknown>,\n  status: number,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: ProviderId,\n  latencyMs: number,\n): Response {\n  return jsonResponse(value, status, {\n    "X-XGuard-Auto-Invoked": "true",\n    "X-XGuard-Request-Id": requestId,\n    "X-XGuard-Fee-Micro-Usd": String(feeMicroUsd),\n    "X-XGuard-Provider": provider,\n    "X-XGuard-Upstream-Latency-Ms": String(latencyMs),\n  });\n}`;
const autoNew = `function autoResponse(\n  value: Record<string, unknown>,\n  status: number,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: ProviderId,\n  latencyMs: number,\n  accounting?: AutoInvokeAccountingState,\n): Response {\n  const headers: Record<string, string> = {\n    "X-XGuard-Auto-Invoked": "true",\n    "X-XGuard-Request-Id": requestId,\n    "X-XGuard-Fee-Micro-Usd": String(feeMicroUsd),\n    "X-XGuard-Provider": provider,\n    "X-XGuard-Upstream-Latency-Ms": String(latencyMs),\n  };\n  if (accounting !== undefined) headers["X-XGuard-Accounting"] = accounting;\n  return jsonResponse(value, status, headers);\n}`;
if (source.includes(autoOld)) source = source.replace(autoOld, autoNew);
else if (!source.includes("accounting?: AutoInvokeAccountingState"))
  throw new Error("autoResponse signature not found");

// Enforce the JSON cap on streamed bytes even when Content-Length is absent.
const jsonOld = `async function readModel(request: Request): Promise<string | null> {\n  if (request.method === "GET" || request.method === "HEAD") return null;\n  const contentType = request.headers.get("content-type")?.toLowerCase() ?? "";\n  if (!contentType.includes("application/json")) return null;\n  const rawLength = request.headers.get("content-length");\n  if (rawLength === null || !/^[0-9]+$/.test(rawLength)) return null;\n  const declared = Number(rawLength);\n  if (\n    !Number.isSafeInteger(declared) ||\n    declared < 0 ||\n    declared > MAX_JSON_BODY_BYTES\n  )\n    return null;\n  try {\n    const value = (await request.clone().json()) as unknown;\n    if (typeof value !== "object" || value === null || Array.isArray(value))\n      return null;\n    const model = (value as Record<string, unknown>).model;\n    return typeof model === "string" ? model.trim() : null;\n  } catch {\n    return null;\n  }\n}\n\nasync function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const declared = contentLength(request.headers);\n  if (declared > MAX_JSON_BODY_BYTES) throw new Error("request_body_too_large");\n  const value = (await request.json()) as unknown;\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}`;
const jsonNew = `async function readModel(request: Request): Promise<string | null> {\n  if (request.method === "GET" || request.method === "HEAD") return null;\n  const contentType = request.headers.get("content-type")?.toLowerCase() ?? "";\n  if (!contentType.includes("application/json")) return null;\n  try {\n    const value = await boundedJsonValue(request, true);\n    if (typeof value !== "object" || value === null || Array.isArray(value))\n      return null;\n    const model = (value as Record<string, unknown>).model;\n    return typeof model === "string" ? model.trim() : null;\n  } catch {\n    return null;\n  }\n}\n\nasync function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const value = await boundedJsonValue(request, false);\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}\n\nasync function boundedJsonValue(\n  request: Request,\n  cloneRequest: boolean,\n): Promise<unknown> {\n  const target = cloneRequest ? request.clone() : request;\n  const rawLength = target.headers.get("content-length");\n  if (rawLength !== null) {\n    if (!/^[0-9]+$/.test(rawLength)) throw new Error("invalid_content_length");\n    const declared = Number(rawLength);\n    if (\n      !Number.isSafeInteger(declared) ||\n      declared < 0 ||\n      declared > MAX_JSON_BODY_BYTES\n    )\n      throw new Error("request_body_too_large");\n  }\n  if (target.body === null) throw new Error("json_body_required");\n\n  const reader = target.body.getReader();\n  const chunks: Uint8Array[] = [];\n  let total = 0;\n  try {\n    while (true) {\n      const { done, value } = await reader.read();\n      if (done) break;\n      total += value.byteLength;\n      if (total > MAX_JSON_BODY_BYTES) {\n        await reader.cancel().catch(() => undefined);\n        throw new Error("request_body_too_large");\n      }\n      chunks.push(value);\n    }\n  } finally {\n    reader.releaseLock();\n  }\n\n  const bytes = new Uint8Array(total);\n  let offset = 0;\n  for (const chunk of chunks) {\n    bytes.set(chunk, offset);\n    offset += chunk.byteLength;\n  }\n  return JSON.parse(new TextDecoder().decode(bytes)) as unknown;\n}`;
if (source.includes(jsonOld)) source = source.replace(jsonOld, jsonNew);
else if (!source.includes("async function boundedJsonValue"))
  throw new Error("JSON reader block not found");

writeFileSync(sourcePath, source);
