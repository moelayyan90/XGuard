import { readFileSync, writeFileSync } from "node:fs";

const sourcePath = "apps/worker/src/auto-invoke.ts";
let source = readFileSync(sourcePath, "utf8");

const oldReadModel = `async function readModel(request: Request): Promise<string | null> {\n  if (request.method === "GET" || request.method === "HEAD") return null;\n  const contentType = request.headers.get("content-type")?.toLowerCase() ?? "";\n  if (!contentType.includes("application/json")) return null;\n  const declared = contentLength(request.headers);\n  if (declared > MAX_JSON_BODY_BYTES) return null;\n  try {\n    const value = (await request.clone().json()) as unknown;\n    if (typeof value !== "object" || value === null || Array.isArray(value))\n      return null;\n    const model = (value as Record<string, unknown>).model;\n    return typeof model === "string" ? model.trim() : null;\n  } catch {\n    return null;\n  }\n}\n\nasync function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const declared = contentLength(request.headers);\n  if (declared > MAX_JSON_BODY_BYTES) throw new Error("request_body_too_large");\n  const value = (await request.json()) as unknown;\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}`;
const newReadModel = `async function readModel(request: Request): Promise<string | null> {\n  if (request.method === "GET" || request.method === "HEAD") return null;\n  const contentType = request.headers.get("content-type")?.toLowerCase() ?? "";\n  if (!contentType.includes("application/json")) return null;\n  try {\n    const value = await boundedJsonValue(request, true);\n    if (typeof value !== "object" || value === null || Array.isArray(value))\n      return null;\n    const model = (value as Record<string, unknown>).model;\n    return typeof model === "string" ? model.trim() : null;\n  } catch {\n    return null;\n  }\n}\n\nasync function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const value = await boundedJsonValue(request, false);\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}\n\nasync function boundedJsonValue(\n  request: Request,\n  cloneRequest: boolean,\n): Promise<unknown> {\n  const target = cloneRequest ? request.clone() : request;\n  const declared = contentLength(target.headers);\n  if (declared > MAX_JSON_BODY_BYTES) throw new Error("request_body_too_large");\n  if (target.body === null) throw new Error("json_body_required");\n\n  const reader = target.body.getReader();\n  const chunks: Uint8Array[] = [];\n  let total = 0;\n  try {\n    while (true) {\n      const { done, value } = await reader.read();\n      if (done) break;\n      total += value.byteLength;\n      if (total > MAX_JSON_BODY_BYTES) {\n        await reader.cancel().catch(() => undefined);\n        throw new Error("request_body_too_large");\n      }\n      chunks.push(value);\n    }\n  } finally {\n    reader.releaseLock();\n  }\n\n  const bytes = new Uint8Array(total);\n  let offset = 0;\n  for (const chunk of chunks) {\n    bytes.set(chunk, offset);\n    offset += chunk.byteLength;\n  }\n  return JSON.parse(new TextDecoder().decode(bytes)) as unknown;\n}`;
if (source.includes(oldReadModel)) source = source.replace(oldReadModel, newReadModel);
else if (!source.includes("async function boundedJsonValue")) throw new Error("JSON reader block not found");
writeFileSync(sourcePath, source);

const testPath = "tests/auto-invoke.test.ts";
let test = readFileSync(testPath, "utf8");
const marker = `\n  it("recognizes Anthropic native SDK traffic without an XGuard-specific route", async () => {`;
const block = `\n  it("does not parse an oversized chunked-style JSON body for model inference", async () => {\n    const body = JSON.stringify({\n      model: "gemini-3.6-flash",\n      padding: "x".repeat(70 * 1024),\n    });\n    const request = new Request(\`${'${ORIGIN}'}/v1/chat/completions\`, {\n      method: "POST",\n      headers: {\n        "Content-Type": "application/json",\n        "X-Api-Key": \`xg_live_${'${"a".repeat(48)}'}\`,\n      },\n      body,\n    });\n    expect(request.headers.get("content-length")).toBeNull();\n\n    const route = await classifyAutoInvokeRoute(request);\n    expect(route).toMatchObject({\n      provider: "openai",\n      upstreamUrl: "https://api.openai.com/v1/chat/completions",\n    });\n  });\n`;
if (!test.includes("does not parse an oversized chunked-style JSON body for model inference")) {
  if (!test.includes(marker)) throw new Error("bounded JSON test insertion marker not found");
  test = test.replace(marker, block + marker);
}
writeFileSync(testPath, test);
