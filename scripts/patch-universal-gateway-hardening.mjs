import { readFileSync, writeFileSync } from "node:fs";

const path = "apps/worker/src/universal-gateway.ts";
let source = readFileSync(path, "utf8");

const providerCatchOld = `  } catch {\n    await releaseGatewayFee(env.DB, merchantId, reserved.eventKey).catch(\n      () => undefined,\n    );\n    return gatewayResponse(\n      { error: "upstream_unavailable", provider: provider.id },\n      502,\n      requestId,\n      0,\n    );\n  }`;
const providerCatchNew = `  } catch {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return gatewayResponse(\n      { error: "upstream_unavailable", provider: provider.id },\n      502,\n      requestId,\n      0,\n      { "X-XGuard-Accounting": accounting },\n    );\n  }`;
if (source.includes(providerCatchOld)) source = source.replace(providerCatchOld, providerCatchNew);
else if (!source.includes(providerCatchNew)) throw new Error("provider catch block not found");

const providerStatusOld = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (!isBillableGatewayStatus(upstream.status)) {\n    await releaseGatewayFee(env.DB, merchantId, reserved.eventKey).catch(\n      () => undefined,\n    );\n    return proxiedResponse(upstream, requestId, 0, provider.id, latencyMs);\n  }\n\n  await earnGatewayFee(env.DB, {\n    merchantId,\n    eventKey: reserved.eventKey,\n    upstreamStatus: upstream.status,\n    latencyMs,\n    requestBytes: contentLength(request.headers),\n    responseBytes: contentLength(upstream.headers),\n  });\n  return proxiedResponse(\n    upstream,\n    requestId,\n    feeMicroUsd,\n    provider.id,\n    latencyMs,\n  );`;
const providerStatusNew = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (isGatewayRedirectStatus(upstream.status)) {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return gatewayResponse(\n      {\n        error: "upstream_redirect_rejected",\n        provider: provider.id,\n        upstreamStatus: upstream.status,\n      },\n      502,\n      requestId,\n      0,\n      { "X-XGuard-Accounting": accounting },\n    );\n  }\n\n  if (!isBillableGatewayStatus(upstream.status)) {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return proxiedResponse(\n      upstream,\n      requestId,\n      0,\n      provider.id,\n      latencyMs,\n      accounting,\n    );\n  }\n\n  const accounting = await finalizeGatewayExecutionSuccess(env.DB, {\n    merchantId,\n    eventKey: reserved.eventKey,\n    upstreamStatus: upstream.status,\n    latencyMs,\n    requestBytes: contentLength(request.headers),\n    responseBytes: contentLength(upstream.headers),\n  });\n  return proxiedResponse(\n    upstream,\n    requestId,\n    accounting === "earned" ? feeMicroUsd : 0,\n    provider.id,\n    latencyMs,\n    accounting,\n  );`;
if (source.includes(providerStatusOld)) source = source.replace(providerStatusOld, providerStatusNew);
else if (!source.includes("upstream_redirect_rejected") || !source.includes("finalizeGatewayExecutionSuccess")) throw new Error("provider status block not found");

const sourceCatchOld = `  } catch {\n    await releaseGatewayFee(env.DB, merchantId, reserved.eventKey).catch(\n      () => undefined,\n    );\n    return gatewayResponse(\n      { error: "source_search_unavailable" },\n      503,\n      requestId,\n      0,\n    );\n  }`;
const sourceCatchNew = `  } catch {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return gatewayResponse(\n      { error: "source_search_unavailable" },\n      503,\n      requestId,\n      0,\n      { "X-XGuard-Accounting": accounting },\n    );\n  }`;
if (source.includes(sourceCatchOld)) source = source.replace(sourceCatchOld, sourceCatchNew);
else if (!source.includes(sourceCatchNew)) throw new Error("source catch block not found");

const sourceStatusOld = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (!sourceResponse.ok) {\n    await releaseGatewayFee(env.DB, merchantId, reserved.eventKey).catch(\n      () => undefined,\n    );\n    return proxiedResponse(\n      sourceResponse,\n      requestId,\n      0,\n      "xguard-catalog",\n      latencyMs,\n    );\n  }\n  await earnGatewayFee(env.DB, {\n    merchantId,\n    eventKey: reserved.eventKey,\n    upstreamStatus: sourceResponse.status,\n    latencyMs,\n    responseBytes: contentLength(sourceResponse.headers),\n  });\n  return proxiedResponse(\n    sourceResponse,\n    requestId,\n    feeMicroUsd,\n    "xguard-catalog",\n    latencyMs,\n  );`;
const sourceStatusNew = `  const latencyMs = Math.max(0, Date.now() - started);\n  if (isGatewayRedirectStatus(sourceResponse.status)) {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return gatewayResponse(\n      {\n        error: "source_redirect_rejected",\n        upstreamStatus: sourceResponse.status,\n      },\n      502,\n      requestId,\n      0,\n      { "X-XGuard-Accounting": accounting },\n    );\n  }\n  if (!sourceResponse.ok) {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return proxiedResponse(\n      sourceResponse,\n      requestId,\n      0,\n      "xguard-catalog",\n      latencyMs,\n      accounting,\n    );\n  }\n  const accounting = await finalizeGatewayExecutionSuccess(env.DB, {\n    merchantId,\n    eventKey: reserved.eventKey,\n    upstreamStatus: sourceResponse.status,\n    latencyMs,\n    responseBytes: contentLength(sourceResponse.headers),\n  });\n  return proxiedResponse(\n    sourceResponse,\n    requestId,\n    accounting === "earned" ? feeMicroUsd : 0,\n    "xguard-catalog",\n    latencyMs,\n    accounting,\n  );`;
if (source.includes(sourceStatusOld)) source = source.replace(sourceStatusOld, sourceStatusNew);
else if (!source.includes("source_redirect_rejected")) throw new Error("source status block not found");

const localOld = `  const started = Date.now();\n  try {\n    const result = await execute();\n    const latencyMs = Math.max(0, Date.now() - started);\n    await earnGatewayFee(env.DB, {\n      merchantId,\n      eventKey: reserved.eventKey,\n      upstreamStatus: 200,\n      latencyMs,\n      requestBytes: contentLength(request.headers),\n    });\n    return gatewayResponse(result, 200, requestId, feeMicroUsd, {\n      "X-XGuard-Gateway-Kind": kind.toLowerCase(),\n    });\n  } catch (error) {\n    await releaseGatewayFee(env.DB, merchantId, reserved.eventKey).catch(\n      () => undefined,\n    );\n    return gatewayResponse({ error: errorCode(error) }, 400, requestId, 0);\n  }`;
const localNew = `  const started = Date.now();\n  let result: Record<string, unknown>;\n  try {\n    result = await execute();\n  } catch (error) {\n    const accounting = await releaseGatewayExecutionReservation(\n      env.DB,\n      merchantId,\n      reserved.eventKey,\n    );\n    return gatewayResponse({ error: errorCode(error) }, 400, requestId, 0, {\n      "X-XGuard-Accounting": accounting,\n    });\n  }\n\n  const latencyMs = Math.max(0, Date.now() - started);\n  const accounting = await finalizeGatewayExecutionSuccess(env.DB, {\n    merchantId,\n    eventKey: reserved.eventKey,\n    upstreamStatus: 200,\n    latencyMs,\n    requestBytes: contentLength(request.headers),\n  });\n  return gatewayResponse(\n    result,\n    200,\n    requestId,\n    accounting === "earned" ? feeMicroUsd : 0,\n    {\n      "X-XGuard-Gateway-Kind": kind.toLowerCase(),\n      "X-XGuard-Accounting": accounting,\n    },\n  );`;
if (source.includes(localOld)) source = source.replace(localOld, localNew);
else if (!source.includes('"X-XGuard-Gateway-Kind": kind.toLowerCase(),\n      "X-XGuard-Accounting": accounting')) throw new Error("local operation block not found");

const billable = `export function isBillableGatewayStatus(status: number): boolean {\n  return Number.isInteger(status) && status >= 200 && status < 300;\n}\n`;
const helpers = `${billable}\nexport function isGatewayRedirectStatus(status: number): boolean {\n  return Number.isInteger(status) && status >= 300 && status < 400;\n}\n\nexport type GatewayExecutionAccountingState =\n  | "earned"\n  | "released"\n  | "pending-release";\n\nexport async function finalizeGatewayExecutionSuccess(\n  db: D1Database,\n  input: {\n    merchantId: string;\n    eventKey: string;\n    upstreamStatus: number;\n    latencyMs: number;\n    requestBytes?: number;\n    responseBytes?: number;\n  },\n): Promise<GatewayExecutionAccountingState> {\n  try {\n    await earnGatewayFee(db, input);\n    return "earned";\n  } catch {\n    return releaseGatewayExecutionReservation(\n      db,\n      input.merchantId,\n      input.eventKey,\n    );\n  }\n}\n\nexport async function releaseGatewayExecutionReservation(\n  db: D1Database,\n  merchantId: string,\n  eventKey: string,\n): Promise<Exclude<GatewayExecutionAccountingState, "earned">> {\n  try {\n    await releaseGatewayFee(db, merchantId, eventKey);\n    return "released";\n  } catch {\n    return "pending-release";\n  }\n}\n`;
if (!source.includes("export async function finalizeGatewayExecutionSuccess")) {
  if (!source.includes(billable)) throw new Error("billable helper not found");
  source = source.replace(billable, helpers);
}

const proxySigOld = `function proxiedResponse(\n  upstream: Response,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: string,\n  latencyMs: number,\n): Response {`;
const proxySigNew = `function proxiedResponse(\n  upstream: Response,\n  requestId: string,\n  feeMicroUsd: number,\n  provider: string,\n  latencyMs: number,\n  accounting: GatewayExecutionAccountingState,\n): Response {`;
if (source.includes(proxySigOld)) source = source.replace(proxySigOld, proxySigNew);
else if (!source.includes(proxySigNew)) throw new Error("proxied response signature not found");

const proxyHeaderOld = `  headers.set("X-XGuard-Upstream-Latency-Ms", String(latencyMs));\n  headers.set("X-Content-Type-Options", "nosniff");`;
const proxyHeaderNew = `  headers.set("X-XGuard-Upstream-Latency-Ms", String(latencyMs));\n  headers.set("X-XGuard-Accounting", accounting);\n  headers.set("X-Content-Type-Options", "nosniff");`;
if (source.includes(proxyHeaderOld)) source = source.replace(proxyHeaderOld, proxyHeaderNew);
else if (!source.includes('headers.set("X-XGuard-Accounting", accounting)')) throw new Error("proxied response headers not found");

const jsonOld = `async function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const contentType = request.headers\n    .get("content-type")\n    ?.split(";", 1)[0]\n    ?.toLowerCase();\n  if (contentType !== "application/json")\n    throw new Error("application_json_required");\n  const declared = contentLength(request.headers);\n  if (declared > MAX_JSON_BODY_BYTES) throw new Error("request_body_too_large");\n  const text = await request.text();\n  if (text.length > MAX_JSON_BODY_BYTES)\n    throw new Error("request_body_too_large");\n  const value = JSON.parse(text) as unknown;\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}`;
const jsonNew = `async function jsonObject(request: Request): Promise<Record<string, unknown>> {\n  const contentType = request.headers\n    .get("content-type")\n    ?.split(";", 1)[0]\n    ?.toLowerCase();\n  if (contentType !== "application/json")\n    throw new Error("application_json_required");\n  const text = await boundedRequestText(request, MAX_JSON_BODY_BYTES);\n  const value = JSON.parse(text) as unknown;\n  if (typeof value !== "object" || value === null || Array.isArray(value))\n    throw new Error("json_object_required");\n  return value as Record<string, unknown>;\n}\n\nasync function boundedRequestText(request: Request, maxBytes: number): Promise<string> {\n  const rawLength = request.headers.get("content-length");\n  if (rawLength !== null) {\n    if (!/^[0-9]+$/.test(rawLength)) throw new Error("invalid_content_length");\n    const declared = Number(rawLength);\n    if (!Number.isSafeInteger(declared) || declared < 0 || declared > maxBytes)\n      throw new Error("request_body_too_large");\n  }\n  if (request.body === null) return "";\n  const reader = request.body.getReader();\n  const chunks: Uint8Array[] = [];\n  let total = 0;\n  try {\n    while (true) {\n      const { done, value } = await reader.read();\n      if (done) break;\n      total += value.byteLength;\n      if (total > maxBytes) {\n        void reader.cancel().catch(() => undefined);\n        throw new Error("request_body_too_large");\n      }\n      chunks.push(value);\n    }\n  } finally {\n    reader.releaseLock();\n  }\n  const bytes = new Uint8Array(total);\n  let offset = 0;\n  for (const chunk of chunks) {\n    bytes.set(chunk, offset);\n    offset += chunk.byteLength;\n  }\n  return new TextDecoder().decode(bytes);\n}`;
if (source.includes(jsonOld)) source = source.replace(jsonOld, jsonNew);
else if (!source.includes("async function boundedRequestText")) throw new Error("JSON block not found");

writeFileSync(path, source);
