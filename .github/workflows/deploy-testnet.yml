name: Legacy XGuard inference testnet — retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy inference deployment is retired. XGuard production is Email Shield."
