name: Sync shadow observe activation

on:
  push:
    branches:
      - agent/economic-firewall-shadow-observe
    paths:
      - .github/workflows/sync-shadow-observe.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/economic-firewall-shadow-observe
          fetch-depth: 0
      - name: Merge latest main
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
          git push origin HEAD:agent/economic-firewall-shadow-observe
