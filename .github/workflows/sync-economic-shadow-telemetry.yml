name: Sync Economic Firewall telemetry

on:
  push:
    branches:
      - agent/economic-shadow-telemetry
    paths:
      - .github/workflows/sync-economic-shadow-telemetry.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/economic-shadow-telemetry
          fetch-depth: 0
      - name: Merge latest main
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
          git push origin HEAD:agent/economic-shadow-telemetry
