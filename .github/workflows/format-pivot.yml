name: Archived pivot formatter

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary formatter archived after pivot cleanup."
