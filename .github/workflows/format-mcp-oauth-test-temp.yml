name: Format MCP OAuth test temporary

on:
  pull_request:
    paths:
      - "tests/worker/mcp-oauth.test.ts"
      - ".github/workflows/format-mcp-oauth-test-temp.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx prettier --write tests/worker/mcp-oauth.test.ts
      - name: Commit formatted test
        shell: bash
        run: |
          if git diff --quiet -- tests/worker/mcp-oauth.test.ts; then
            exit 0
          fi
          git config user.name "xguard-ci"
          git config user.email "actions@users.noreply.github.com"
          git add tests/worker/mcp-oauth.test.ts
          git commit -m "format MCP OAuth test"
          git push origin "HEAD:${GITHUB_HEAD_REF}"
