name: Format universal router branch

on:
  push:
    branches:
      - agent/universal-protocol-router

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/universal-protocol-router
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Format changed universal-router files
        run: |
          npx prettier --write \
            apps/worker/src/universal-protocol-router.ts \
            apps/worker/src/universal-mainnet.ts \
            apps/worker/src/generic-http-connector.ts \
            apps/worker/wrangler.mainnet.jsonc \
            tests/universal-protocol-router.test.ts \
            tests/generic-http-connector.test.ts
      - name: Commit formatting if needed
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/worker/src/universal-protocol-router.ts \
            apps/worker/src/universal-mainnet.ts \
            apps/worker/src/generic-http-connector.ts \
            apps/worker/wrangler.mainnet.jsonc \
            tests/universal-protocol-router.test.ts \
            tests/generic-http-connector.test.ts
          git commit -m "format universal router changes"
          git push origin HEAD:agent/universal-protocol-router
