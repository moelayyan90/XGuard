name: Economic Firewall preview verification — disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "XGuard economic preview deployment is intentionally disabled during shutdown."
