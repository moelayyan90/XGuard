name: Patch auto invoke bounded JSON once

on:
  push:
    branches: [agent/zero-study-auto-invoke-v2]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/zero-study-auto-invoke-v2
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false
      - run: npm ci --ignore-scripts
      - run: node scripts/patch-auto-invoke-bounded-json.mjs
      - run: npx prettier --write apps/worker/src/auto-invoke.ts tests/auto-invoke.test.ts
      - run: npm run build
      - run: npx vitest run tests/auto-invoke.test.ts
      - run: npm run typecheck
      - name: Commit bounded JSON hardening
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/worker/src/auto-invoke.ts tests/auto-invoke.test.ts
          git commit -m "Bound auto-invoke JSON reads independently of Content-Length"
          git push origin HEAD:agent/zero-study-auto-invoke-v2
